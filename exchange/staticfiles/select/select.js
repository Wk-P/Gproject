const exchangeDropdown = document.getElementById('exchange-dropdown');

exchangeDropdown.addEventListener('change', function() {
  const selectedExchange = exchangeDropdown.value;
  
  // Do something with the selected exchange, such as displaying information or making a request to a server
});
